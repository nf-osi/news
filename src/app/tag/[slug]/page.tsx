// Singular alias for /tags/<slug>, matching the WordPress-style tag URL that
// the NF portal's RssFeedCards component hardcodes for category badge links.
export { default, generateMetadata, generateStaticParams } from "@/app/tags/[slug]/page";
